classdef prtDataInterfaceTargets
        
    methods (Hidden)
        %this is used by some class objects, but breaks encapsulation. Hide
        %it from 99% of users
        function self = updateTargetCache(self)
            
        end
    end
    
end