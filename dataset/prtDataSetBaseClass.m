classdef prtDataSetBaseClass
    
    methods (Abstract)
        %What do CLASSIFIERS NEED?
        %somehow we lost track of this
        
        %this is what we check when a classifier run gets a dataset!
    end
end
